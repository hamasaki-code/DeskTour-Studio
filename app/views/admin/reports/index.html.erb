<% content_for :title, "Report Queue" %>

<section class="space-y-6">
  <div class="flex flex-wrap items-center justify-between gap-3">
    <h1 class="text-2xl font-bold text-slate-900">Report Queue</h1>
    <%= link_to "Post Management", admin_posts_path, class: "rounded border border-slate-300 px-3 py-1 text-xs hover:bg-slate-100" %>
  </div>

  <div class="overflow-x-auto rounded-xl border border-slate-200 bg-white">
    <table class="min-w-full text-left text-sm">
      <thead class="bg-slate-50 text-slate-600">
        <tr>
          <th class="px-4 py-3">Post</th>
          <th class="px-4 py-3">Reason</th>
          <th class="px-4 py-3">Reporter</th>
          <th class="px-4 py-3">Details</th>
          <th class="px-4 py-3">Reported At</th>
          <th class="px-4 py-3">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if @queued_reports.any? %>
          <% @queued_reports.each do |report| %>
            <tr class="border-t border-slate-200 align-top">
              <td class="px-4 py-3">
                <% if report.post&.published? %>
                  <%= link_to report.post.title, post_path(report.post), class: "text-blue-600 hover:text-blue-700" %>
                <% else %>
                  <span><%= report.post&.title.presence || "(Deleted post)" %></span>
                <% end %>
                <% if report.post&.hidden? %>
                  <p class="mt-1 text-xs text-amber-700">Currently hidden</p>
                <% end %>
              </td>
              <td class="px-4 py-3"><%= Report::REASON_LABELS.fetch(report.reason.to_sym) %></td>
              <td class="px-4 py-3"><%= report.reporter_user&.name.presence || "Anonymous" %></td>
              <td class="px-4 py-3"><%= truncate(report.details.to_s, length: 120) %></td>
              <td class="px-4 py-3"><%= l(report.created_at, format: :short) %></td>
              <td class="px-4 py-3">
                <div class="flex flex-wrap gap-2">
                  <%= button_to "Restore", restore_post_admin_report_path(report), method: :patch, class: "rounded border border-emerald-300 px-3 py-1 text-xs text-emerald-700 hover:bg-emerald-50" %>
                  <%= button_to "Dismiss", dismiss_admin_report_path(report), method: :patch, class: "rounded border border-slate-300 px-3 py-1 text-xs hover:bg-slate-100" %>
                  <%= button_to "Delete Post", delete_post_admin_report_path(report), method: :delete, data: { turbo_confirm: "Delete this post permanently?" }, class: "rounded border border-red-300 px-3 py-1 text-xs text-red-700 hover:bg-red-50" %>
                </div>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr class="border-t border-slate-200">
            <td colspan="6" class="px-4 py-6 text-center text-slate-500">No queued reports.</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="overflow-x-auto rounded-xl border border-slate-200 bg-white">
    <div class="border-b border-slate-200 px-4 py-3">
      <h2 class="text-sm font-semibold text-slate-900">Recently reviewed</h2>
    </div>
    <table class="min-w-full text-left text-sm">
      <thead class="bg-slate-50 text-slate-600">
        <tr>
          <th class="px-4 py-3">Post</th>
          <th class="px-4 py-3">Reason</th>
          <th class="px-4 py-3">Status</th>
          <th class="px-4 py-3">Reviewed At</th>
        </tr>
      </thead>
      <tbody>
        <% if @reviewed_reports.any? %>
          <% @reviewed_reports.each do |report| %>
            <tr class="border-t border-slate-200">
              <td class="px-4 py-3"><%= report.post&.title.presence || "(Deleted post)" %></td>
              <td class="px-4 py-3"><%= Report::REASON_LABELS.fetch(report.reason.to_sym) %></td>
              <td class="px-4 py-3"><%= report.status %></td>
              <td class="px-4 py-3"><%= report.reviewed_at.present? ? l(report.reviewed_at, format: :short) : "-" %></td>
            </tr>
          <% end %>
        <% else %>
          <tr class="border-t border-slate-200">
            <td colspan="4" class="px-4 py-6 text-center text-slate-500">No reviewed reports.</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</section>
